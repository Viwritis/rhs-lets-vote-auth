apply plugin: "war"

bootWar {
    mainClassName = "pl.redheadsolutions.lg.lets.vote.auth.LetsVoteOAuthApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
